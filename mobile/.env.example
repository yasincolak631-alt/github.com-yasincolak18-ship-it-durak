# Backend API'sinin adresi. Gerçek cihazda (simülatör/emülatör değil) test ederken
# 'localhost' çalışmaz — bilgisayarının yerel ağ IP'sini kullan (örn. 192.168.1.42).
EXPO_PUBLIC_API_URL="http://localhost:3000"
